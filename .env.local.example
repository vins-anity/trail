# Trail AI Environment Variables Template
# ================================
# Copy this file to .env and fill in your actual values
# NEVER commit .env to version control!

# ===========================================
# Database (Drizzle ORM - from Supabase ORMs tab)
# ===========================================
# Get from: Supabase Dashboard → Settings → Database
DATABASE_URL=postgresql://postgres.[PROJECT-REF]:[YOUR-PASSWORD]@aws-1-ap-south-1.pooler.supabase.com:6543/postgres

# ===========================================
# Supabase API (from Data API & API Keys settings)
# ===========================================
# Get from: Supabase Dashboard → Settings → API
SUPABASE_URL=https://[PROJECT-REF].supabase.co
SUPABASE_ANON_KEY=your_supabase_anon_key_here

# ===========================================
# Webhook Secrets (get from each integration)
# ===========================================

# Slack Configuration
# Get from: https://api.slack.com/apps → Your App → Basic Information
SLACK_SIGNING_SECRET=your_slack_signing_secret
# Get from: https://api.slack.com/apps → Your App → OAuth & Permissions
SLACK_BOT_TOKEN=xoxb-your-slack-bot-token
# Optional (only if using Socket Mode instead of webhooks)
SLACK_APP_TOKEN=xapp-your-slack-app-token

# GitHub App Configuration
# Create app at: https://github.com/settings/apps/new
GITHUB_APP_ID=your_github_app_id
GITHUB_CLIENT_ID=your_github_client_id
GITHUB_CLIENT_SECRET=your_github_client_secret
GITHUB_WEBHOOK_SECRET=your_github_webhook_secret

# Jira Configuration
# Create OAuth app at: https://developer.atlassian.com/console/myapps/
JIRA_HOST=your-domain.atlassian.net
JIRA_CLIENT_ID=your_jira_client_id
JIRA_CLIENT_SECRET=your_jira_client_secret
JIRA_WEBHOOK_SECRET=your_jira_webhook_secret

# ===========================================
# AI Configuration
# ===========================================
# Get from: https://aistudio.google.com/app/apikey
GEMINI_API_KEY=your_gemini_api_key_here

# ===========================================
# Security
# ===========================================
# 256-bit encryption key for OAuth tokens (32 bytes as hex)
# Generate with: bun run -e "console.log(Array.from(crypto.getRandomValues(new Uint8Array(32))).map(b => b.toString(16).padStart(2, '0')).join(''))"
ENCRYPTION_KEY=

# ===========================================
# Application Settings
# ===========================================
NODE_ENV=development
PORT=3000

# Authentication
